#include <iostream>
#include <vector>
#include <queue>

using namespace std;
vector<pair<int, int>> vec;
int N, K;
int Func(){

    
    return 0;
}

int main()
{
    cin >> N >> K;
    int a,b;

    for(int i = 0; i < N; i++){
        vec.push_back(make_pair(a,b));
    }
    int answer = Func();

    return 0;
}